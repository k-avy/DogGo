{{template "base" .}}

{{define "title"}}Quote{{end}}

{{define "main"}}
<div class="card">
    <div class="card-body">
        <blockquote class="blockquote mb-0">
            <p>{{ .GetQuote }}</p>

            <footer class="blockquote-footer">
                
            </footer>
        </blockquote>
    </div>
    <div class="card-footer">

        <button class="btn btn-outline-secondary" type="button" id="quotebutt" onclick="window.location.reload();">New
            Quote</button>


    </div>
</div>
</div>

{{end}}